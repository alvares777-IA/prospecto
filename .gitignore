# Segredos — nunca versionar com valores reais
.env

# Backups de configuração copiados do servidor
*.conf.20*
httpd.conf.*
dads.conf.*

# Claude Code
.claude/
node_modules/

# Backups gerados por scripts/backup.sh (contém dados reais)
backups/
